const sum = (num1, num2) => {
  return num1 + num2
};


const multiple = (num1, num2) => {
  return num1 * num2
};

const divide = (num1, num2) => {
  return num1 / num2
}







module.exports = {
  sum: sum,
  multiple: multiple,
  divide: divide,
}
